# Supabase (server-side only — never expose the service-role key to the client).
# Leave these unset to run locally with logging disabled (graceful no-op).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Salt mixed into the IP hash before storage. Set to any long random string.
IP_HASH_SALT=
